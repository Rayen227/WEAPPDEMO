<!-- 上方 -->
<view class="nav">
    <view bind:tap="showCompleted">历史错题</view>
    <view bind:tap="showMistaken">错题</view>
    <view bind:tap="showCollected">收藏</view>
</view>
<!-- 中部 -->
<view class="problem">
    <view wx:for="{{wordList}}" wx:for-index="index" wx:for-item="item" data-index="{{index}}" bind:tap="showDetails">
        {{item.en}}
    </view>
</view>