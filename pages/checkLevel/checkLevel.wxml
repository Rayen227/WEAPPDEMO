<!-- pages/checkLevel/checkLevel.wxml -->
<view class="checkbox">
    <view class="check" data-id="{{item}}" bind:tap="select" wx:for="{{levelItems}}">
        <view>{{item+1}}</view>
    </view>
</view>