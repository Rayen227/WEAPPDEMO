<!-- pages/checkLevel/checkLevel.wxml -->
<view class="checkbox">
    <view class="check" wx:for="{{levelItems}}">
        <view data-id="{{item}}" bind:tap="select">{{item}}</view>
    </view>
</view>